# Todo List

一个无需构建工具的响应式 Todo List 页面。

## 功能

- 新增、完成和删除任务
- 添加任务时可选截止日期，自动显示“今天到期”或“已逾期”
- 截止日期按本地日历日期判断，已完成任务不显示逾期提示；兼容已有无日期任务
- 按全部、待完成和已完成筛选
- 一键清除已完成任务
- 使用 `localStorage` 自动保存
- 移动端适配和键盘可访问性

直接打开 `index.html` 即可使用。
